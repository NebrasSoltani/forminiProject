<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@100;200;300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <title>{% block title %}Formini.tn{% endblock %}</title>
    <link href="{{ asset('vendor/bootstrap/css/bootstrap.min.css') }}" rel="stylesheet">
    <link rel="stylesheet" href="{{ asset('asset/css/fontawesome.css') }}">
    <link rel="stylesheet" href="{{ asset('asset/css/templatemo-onix-digital.css') }}">
    <link rel="stylesheet" href="{{ asset('asset/css/animated.css') }}">
    <link rel="stylesheet" href="{{ asset('asset/css/owl.css') }}">
    <link rel="icon" href="{{ asset('asset/images/favicon.png') }}" type="image/png">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <style>
      /* Header toujours lisible sur les pages sans bannière */
      .header-area.header-sticky {
        background: #fff !important;
        box-shadow: 0 1px 10px rgba(0,0,0,.08);
      }
      .header-area .main-nav .nav li a {
        color: #2a2a2a !important;
      }
      .header-area .main-nav .nav li a:hover {
        color: #6974e8 !important;
      }
      .main-content {
        min-height: 60vh;
      }
    </style>
    {% block stylesheets %}{% endblock %}
</head>
<body>
  <div id="js-preloader" class="js-preloader">
    <div class="preloader-inner">
      <span class="dot"></span>
      <div class="dots"><span></span><span></span><span></span></div>
    </div>
  </div>

  <header class="header-area header-sticky wow slideInDown" data-wow-duration="0.75s" data-wow-delay="0s">
    <div class="container">
      <div class="row">
        <div class="col-12">
          <nav class="main-nav">
            <a href="{{ path('accueil') }}" class="logo">
              <img src="{{ asset('asset/images/logo.png') }}" alt="Formini" style="width: 200px; height: 75px;">
            </a>
            <ul class="nav">
              <li class="scroll-to-section"><a href="{{ path('accueil') }}">Accueil</a></li>
              <li class="scroll-to-section"><a href="{{ path('accueil') }}#services">Formations</a></li>
              <li class="scroll-to-section"><a href="{{ path('accueil') }}#evenements">&#201;v&#233;nements</a></li>
              <li class="scroll-to-section"><a href="{{ path('accueil') }}#about">À Propos</a></li>
              <li class="scroll-to-section"><a href="{{ path('accueil') }}#portfolio">Réussites</a></li>
              <li class="scroll-to-section"><a href="{{ path('accueil') }}#video">Nos Programmes</a></li>
              <li class="scroll-to-section"><a href="{{ path('accueil') }}#contact">Contact</a></li>
              {% if app.user %}
                <li class="scroll-to-section dropdown">
                  <a href="#" class="dropdown-toggle d-flex align-items-center gap-2 text-decoration-none text-dark" id="userDropdown" data-bs-toggle="dropdown" aria-expanded="false">
                    {% if app.user.photo %}
                      <img src="{{ asset('uploads/photos/' ~ app.user.photo) }}" alt="{{ app.user.nom }}" class="rounded-circle border border-2 border-primary" style="width: 40px; height: 40px; object-fit: cover;">
                    {% else %}
                      <img src="{{ asset('assets/images/users/profile-pic.jpg') }}" alt="{{ app.user.nom }}" class="rounded-circle border border-2 border-primary" style="width: 40px; height: 40px; object-fit: cover;">
                    {% endif %}
                    <span class="fw-semibold text-dark">{{ app.user.prenom }} {{ app.user.nom }}</span>
                    <i class="fa fa-chevron-down text-dark" style="font-size: 12px;"></i>
                  </a>
                  <ul class="dropdown-menu dropdown-menu-end shadow border-0 mt-2 py-2" aria-labelledby="userDropdown" style="min-width: 220px; border-radius: 10px;">
                    <li>
                      <a class="dropdown-item d-flex align-items-center py-2 px-3 text-dark" href="{% if app.user.roleUtilisateur == 'apprenant' %}{{ path('apprenant_dashboard') }}{% elseif app.user.roleUtilisateur == 'formateur' %}{{ path('formateur_dashboard') }}{% elseif app.user.roleUtilisateur == 'admin' %}{{ path('admin_dashboard') }}{% else %}{{ path('societe_dashboard') }}{% endif %}">
                        <i class="fa fa-tachometer me-2 text-primary"></i>Mon Dashboard
                      </a>
                    </li>
                    <li><hr class="dropdown-divider my-1"></li>
                    <li>
                      <a class="dropdown-item d-flex align-items-center py-2 px-3 text-dark" href="{{ path('app_profile_edit') }}">
                        <i class="fa fa-pencil me-2 text-primary"></i>Modifier le profil
                      </a>
                    </li>
                    <li><hr class="dropdown-divider my-1"></li>
                    <li>
                      <a class="dropdown-item d-flex align-items-center py-2 px-3 text-dark" href="{{ path('app_logout') }}">
                        <i class="fa fa-sign-out me-2 text-danger"></i>Se déconnecter
                      </a>
                    </li>
                  </ul>
                </li>
              {% else %}
                <li class="scroll-to-section"><a href="{{ path('app_login') }}">Connexion</a></li>
                <li class="scroll-to-section">
                  <div class="main-red-button-hover">
                    <a href="{{ path('app_register') }}">Rejoindre Formini</a>
                  </div>
                </li>
              {% endif %}
            </ul>
            <a class="menu-trigger"><span>Menu</span></a>
          </nav>
        </div>
      </div>
    </div>
  </header>

  <div class="main-content py-4 py-md-5">
    {% block body %}{% endblock %}
  </div>

  <footer>
    <div class="container">
      <div class="row">
        <div class="col-lg-3">
          <div class="about footer-item">
            <div class="logo">
              <a href="{{ path('accueil') }}"><img src="{{ asset('asset/images/logo.png') }}" alt="Formini.tn"></a>
            </div>
            <p>Plateforme de formation certifiante en ligne.</p>
          </div>
        </div>
        <div class="col-lg-3">
          <div class="services footer-item">
            <h4>Liens</h4>
            <ul>
              <li><a href="{{ path('accueil') }}">Accueil</a></li>
              <li><a href="{{ path('accueil') }}#evenements">&#201;v&#233;nements</a></li>
              <li><a href="{{ path('accueil') }}#contact">Contact</a></li>
            </ul>
          </div>
        </div>
        <div class="col-lg-12">
          <div class="copyright mt-4">
            <p>© {{ "now"|date("Y") }} Formini.tn - Tous droits réservés.</p>
          </div>
        </div>
      </div>
    </div>
  </footer>

  <script src="{{ asset('vendor/jquery/jquery.min.js') }}"></script>
  <script src="{{ asset('vendor/bootstrap/js/bootstrap.bundle.min.js') }}"></script>
  <script src="{{ asset('asset/js/owl-carousel.js') }}"></script>
  <script src="{{ asset('asset/js/animation.js') }}"></script>
  <script src="{{ asset('asset/js/imagesloaded.js') }}"></script>
  <script src="{{ asset('asset/js/custom.js') }}"></script>
  {% block javascripts %}{% endblock %}
</body>
</html>
