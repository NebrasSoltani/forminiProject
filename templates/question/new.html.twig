{% extends 'base.html.twig' %}

{% block title %}Nouvelle Question - {{ quiz.titre }}{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header">
                    <h2>Créer une nouvelle question</h2>
                    <p class="text-muted mb-0">Quiz : {{ quiz.titre }}</p>
                </div>
                <div class="card-body">
                    {{ form_start(form) }}
                        {{ form_row(form.enonce) }}
                        
                        <div class="row">
                            <div class="col-md-6">
                                {{ form_row(form.type) }}
                            </div>
                            <div class="col-md-3">
                                {{ form_row(form.points) }}
                            </div>
                            <div class="col-md-3">
                                {{ form_row(form.ordre) }}
                            </div>
                        </div>
                        
                        {{ form_row(form.explication) }}
                        
                        <div class="alert alert-info">
                            <strong>💡 Astuce :</strong> Après avoir créé la question, vous pourrez ajouter les réponses possibles.
                        </div>
                        
                        <div class="d-flex justify-content-between mt-3">
                            <a href="{{ path('question_index', {'formationId': formation.id, 'quizId': quiz.id}) }}" class="btn btn-secondary">Annuler</a>
                            <button type="submit" class="btn btn-primary">📝 Créer la question</button>
                        </div>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}