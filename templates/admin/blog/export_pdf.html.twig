<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Export PDF - Blogs</title>
    <style>
        @page {
            margin: 20mm;
            size: A4 landscape;
        }
        
        body {
            font-family: 'DejaVu Sans', Arial, sans-serif;
            font-size: 10px;
            line-height: 1.4;
            color: #333;
        }
        
        .header {
            text-align: center;
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 2px solid #007bff;
        }
        
        .header h1 {
            color: #007bff;
            font-size: 18px;
            margin: 0 0 5px 0;
        }
        
        .header .subtitle {
            color: #666;
            font-size: 11px;
        }
        
        .info-box {
            background-color: #f8f9fa;
            border: 1px solid #dee2e6;
            border-radius: 4px;
            padding: 10px;
            margin-bottom: 15px;
            font-size: 9px;
        }
        
        .info-box strong {
            color: #495057;
        }
        
        .table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 10px;
        }
        
        .table th {
            background-color: #007bff;
            color: white;
            text-align: left;
            padding: 8px;
            font-weight: bold;
            border: 1px solid #0056b3;
        }
        
        .table td {
            padding: 8px;
            border: 1px solid #dee2e6;
            vertical-align: top;
        }
        
        .table tr:nth-child(even) {
            background-color: #f8f9fa;
        }
        
        .table tr:hover {
            background-color: #e9ecef;
        }
        
        .badge {
            display: inline-block;
            padding: 2px 6px;
            font-size: 9px;
            font-weight: bold;
            line-height: 1;
            text-align: center;
            white-space: nowrap;
            vertical-align: baseline;
            border-radius: 3px;
        }
        
        .badge-success {
            background-color: #d4edda;
            color: #155724;
        }
        
        .badge-warning {
            background-color: #fff3cd;
            color: #856404;
        }
        
        .badge-info {
            background-color: #d1ecf1;
            color: #0c5460;
        }
        
        .badge-secondary {
            background-color: #e2e3e5;
            color: #383d41;
        }
        
        .text-center {
            text-align: center;
        }
        
        .text-muted {
            color: #6c757d;
        }
        
        .page-number:after {
            content: "Page " counter(page);
        }
        
        .footer {
            position: fixed;
            bottom: 0;
            left: 0;
            right: 0;
            height: 20mm;
            text-align: center;
            font-size: 8px;
            color: #666;
            border-top: 1px solid #dee2e6;
            padding-top: 5px;
        }
        
        .summary {
            background-color: #e7f1ff;
            border-left: 4px solid #007bff;
            padding: 8px;
            margin-bottom: 15px;
            font-size: 9px;
        }
        
        .summary-title {
            font-weight: bold;
            color: #0056b3;
            margin-bottom: 5px;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>Export des Blogs</h1>
        <div class="subtitle">
            Généré le {{ exportDate|date('d/m/Y à H:i') }} | 
            Total: {{ blogs|length }} blog{% if blogs|length > 1 %}s{% endif %}
        </div>
    </div>
    
    {% if filters is not empty %}
    <div class="summary">
        <div class="summary-title">Filtres appliqués :</div>
        <div>
            {% if filters.search %}
                <strong>Recherche :</strong> "{{ filters.search }}"<br>
            {% endif %}
            {% if filters.categorie %}
                <strong>Catégorie :</strong> {{ filters.categorie }}<br>
            {% endif %}
            {% if filters.isPublie is defined %}
                <strong>Statut :</strong> {{ filters.isPublie == '1' ? 'Publiés' : 'Brouillons' }}<br>
            {% endif %}
            {% if filters.dateFrom %}
                <strong>Du :</strong> {{ filters.dateFrom|date('d/m/Y') }}<br>
            {% endif %}
            {% if filters.dateTo %}
                <strong>Au :</strong> {{ filters.dateTo|date('d/m/Y') }}<br>
            {% endif %}
        </div>
    </div>
    {% endif %}
    
    <table class="table">
        <thead>
            <tr>
                <th width="40">ID</th>
                <th width="200">Titre</th>
                <th width="80">Catégorie</th>
                <th width="100">Auteur</th>
                <th width="70">Date</th>
                <th width="80">Statut</th>
                <th width="100">Événement</th>
                <th>Résumé</th>
            </tr>
        </thead>
        <tbody>
            {% for blog in blogs %}
            <tr>
                <td class="text-center">#{{ blog.id }}</td>
                <td>
                    <strong>{{ blog.titre }}</strong>
                </td>
                <td>
                    <span class="badge badge-info">{{ blog.categorie }}</span>
                </td>
                <td>{{ blog.auteur.prenom }} {{ blog.auteur.nom }}</td>
                <td>{{ blog.datePublication|date('d/m/Y') }}</td>
                <td>
                    <span class="badge {% if blog.isPublie %}badge-success{% else %}badge-warning{% endif %}">
                        {{ blog.isPublie ? 'Publié' : 'Brouillon' }}
                    </span>
                </td>
                <td>
                    {% if blog.evenement %}
                        <span class="badge badge-secondary">{{ blog.evenement.titre }}</span>
                    {% else %}
                        <span class="text-muted">-</span>
                    {% endif %}
                </td>
                <td>{{ blog.resume|default('')|slice(0, 100) }}{% if blog.resume and blog.resume|length > 100 %}...{% endif %}</td>
            </tr>
            {% else %}
            <tr>
                <td colspan="8" class="text-center text-muted" style="padding: 20px;">
                    Aucun blog à afficher
                </td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
    
    <div class="footer">
        Export généré par {{ app.user ? app.user.email : 'Système' }} | 
        {{ "now"|date('d/m/Y H:i:s') }}
    </div>
</body>
</html>