<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Liste des événements - Export PDF</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            font-size: 12px;
            color: #333;
            line-height: 1.4;
        }
        
        .header {
            text-align: center;
            margin-bottom: 30px;
            border-bottom: 2px solid #333;
            padding-bottom: 15px;
        }
        
        .header h1 {
            color: #2c3e50;
            font-size: 24px;
            margin: 0;
        }
        
        .header .subtitle {
            color: #7f8c8d;
            font-size: 14px;
        }
        
        .info-section {
            margin-bottom: 20px;
            background-color: #f8f9fa;
            padding: 15px;
            border-radius: 5px;
        }
        
        .info-grid {
            display: flex;
            justify-content: space-between;
            margin-bottom: 10px;
        }
        
        .info-item {
            flex: 1;
            text-align: center;
        }
        
        .info-label {
            font-weight: bold;
            color: #2c3e50;
            font-size: 10px;
            text-transform: uppercase;
        }
        
        .info-value {
            font-size: 16px;
            color: #27ae60;
        }
        
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 30px;
            page-break-inside: avoid;
        }
        
        th {
            background-color: #2c3e50;
            color: white;
            padding: 10px;
            text-align: left;
            font-size: 11px;
            text-transform: uppercase;
        }
        
        td {
            padding: 8px 10px;
            border-bottom: 1px solid #ddd;
            font-size: 10px;
        }
        
        tr:nth-child(even) {
            background-color: #f8f9fa;
        }
        
        .status-active {
            color: #27ae60;
            font-weight: bold;
        }
        
        .status-inactive {
            color: #e74c3c;
            font-weight: bold;
        }
        
        .badge {
            display: inline-block;
            padding: 3px 8px;
            border-radius: 12px;
            font-size: 10px;
            font-weight: bold;
        }
        
        .badge-conference { background-color: #3498db; color: white; }
        .badge-atelier { background-color: #9b59b6; color: white; }
        .badge-webinaire { background-color: #1abc9c; color: white; }
        .badge-formation { background-color: #f39c12; color: white; }
        .badge-networking { background-color: #e74c3c; color: white; }
        .badge-seminaire { background-color: #34495e; color: white; }
        .badge-hackathon { background-color: #d35400; color: white; }
        .badge-autre { background-color: #95a5a6; color: white; }
        
        .footer {
            text-align: center;
            margin-top: 40px;
            padding-top: 20px;
            border-top: 1px solid #ddd;
            font-size: 10px;
            color: #7f8c8d;
        }
        
        .page-break {
            page-break-before: always;
        }
        
        .section-title {
            font-size: 16px;
            color: #2c3e50;
            margin: 25px 0 15px 0;
            padding-bottom: 8px;
            border-bottom: 1px solid #ddd;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>Liste des événements</h1>
        <div class="subtitle">
            Export généré le {{ dateGeneration|date('d/m/Y H:i') }}
        </div>
    </div>
    
    <div class="info-section">
        <div class="section-title">Statistiques globales</div>
        <div class="info-grid">
            <div class="info-item">
                <div class="info-label">Total</div>
                <div class="info-value">{{ stats.total }}</div>
            </div>
            <div class="info-item">
                <div class="info-label">Actifs</div>
                <div class="info-value">{{ stats.actifs }}</div>
            </div>
            <div class="info-item">
                <div class="info-label">À venir</div>
                <div class="info-value">{{ stats.a_venir }}</div>
            </div>
            <div class="info-item">
                <div class="info-label">En cours</div>
                <div class="info-value">{{ stats.en_cours }}</div>
            </div>
            <div class="info-item">
                <div class="info-label">Passés</div>
                <div class="info-value">{{ stats.passes }}</div>
            </div>
        </div>
    </div>
    
    <div class="section-title">Liste des événements ({{ evenements|length }})</div>
    
    <table>
        <thead>
            <tr>
                <th style="width: 5%;">ID</th>
                <th style="width: 20%;">Titre</th>
                <th style="width: 10%;">Type</th>
                <th style="width: 12%;">Date début</th>
                <th style="width: 12%;">Date fin</th>
                <th style="width: 10%;">Lieu</th>
                <th style="width: 10%;">Places</th>
                <th style="width: 8%;">Statut</th>
                <th style="width: 13%;">Organisateur</th>
            </tr>
        </thead>
        <tbody>
            {% for evenement in evenements %}
            <tr>
                <td>{{ evenement.id }}</td>
                <td>{{ evenement.titre|slice(0, 40) }}{{ evenement.titre|length > 40 ? '...' : '' }}</td>
                <td>
                    <span class="badge badge-{{ evenement.type|lower }}">
                        {{ evenement.type }}
                    </span>
                </td>
                <td>{{ evenement.dateDebut|date('d/m/Y H:i') }}</td>
                <td>{{ evenement.dateFin|date('d/m/Y H:i') }}</td>
                <td>{{ evenement.lieu|slice(0, 20) }}{{ evenement.lieu|length > 20 ? '...' : '' }}</td>
                <td>{{ evenement.nombrePlaces ? evenement.nombrePlaces : 'Illimité' }}</td>
                <td>
                    {% if evenement.isActif %}
                        <span class="status-active">● Actif</span>
                    {% else %}
                        <span class="status-inactive">● Inactif</span>
                    {% endif %}
                </td>
                <td>{{ evenement.organisateur ? evenement.organisateur.nom : 'Non défini' }}</td>
            </tr>
            {% else %}
            <tr>
                <td colspan="9" style="text-align: center; padding: 20px;">
                    Aucun événement trouvé
                </td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
    
    {% if stats.par_type %}
    <div class="section-title">Répartition par type</div>
    <table>
        <thead>
            <tr>
                <th>Type</th>
                <th>Nombre</th>
                <th>Pourcentage</th>
            </tr>
        </thead>
        <tbody>
            {% for type in stats.par_type %}
            <tr>
                <td>{{ type.type ? type.type : 'Non spécifié' }}</td>
                <td>{{ type.nombre }}</td>
                <td>{{ ((type.nombre / stats.total) * 100)|round(1) }}%</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
    {% endif %}
    
    <div class="footer">
        Document généré automatiquement par EventBlogs<br>
        Confidentialité : Ce document est destiné à un usage interne uniquement
    </div>
</body>
</html>